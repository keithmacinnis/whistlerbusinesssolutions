{
  "name": "wbs-homepage",
  "version": "1.0.0",
  "type": "module",
  "description": "Whistler Business Solutions Homepage - Modern Vite Build",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "vite-plugin-html": "^3.2.0"
  }
}

